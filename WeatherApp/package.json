{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "cresting a weather nodejs app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "debayan",
  "license": "ISC",
  "dependencies": {
    "requests": "^0.3.0"
  }
}
